# DeskHog OTA updates

The original OTA implementation plan is now represented by the current-system guide: [OTA updates](docs/ota.md).

That guide covers the active GitHub Releases flow, worker tasks, status model, partition limits, release-asset requirements, security, and recovery.
